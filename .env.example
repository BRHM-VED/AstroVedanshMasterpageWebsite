# API Base URL for backend requests
# In local development, this defaults to '/api' which is proxied to the local backend.
# In production, set this to the full URL of your deployed Laravel API (e.g., https://api.astrovedansh.com/api).
VITE_API_BASE=/api

# Proxy target for local development server (Vite proxy config)
# Change this if your local Laravel server runs on a different port or host.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
